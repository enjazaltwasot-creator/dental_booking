# حزمة نقل موقع مجموعة إيفان الطبية

هذه الحزمة تحتوي على مصدر تطبيق مجموعة إيفان الطبية بعد اجتياز أمر البناء المحلي. يمكن استخدامها كأساس لنقل التطبيق إلى استضافة تدعم Node.js، لكنها ليست رفعاً ثابتاً لملفات HTML فقط؛ فالموقع يتضمن خادماً من Express وواجهة React ونظام حجوزات ولوحة إدارة وقاعدة بيانات MySQL/TiDB.

## ما تتضمنه الحزمة وما لا تتضمنه

| العنصر | الحالة | ملاحظة |
|---|---|---|
| مصدر الواجهة والخادم | مشمول | React، Express، tRPC وDrizzle ORM |
| ملف قفل الحزم | مشمول | `pnpm-lock.yaml` لتثبيت الاعتمادات المتوافقة |
| قالب إعدادات موثق | مشمول في هذا الدليل | تُنشأ متغيرات البيئة من لوحة الاستضافة ولا تحفظ داخل المشروع |
| مفاتيح الإنتاج والأسرار | غير مشمول | يجب إنشاؤها وإدخالها في بيئة الاستضافة فقط |
| قاعدة بيانات الحجوزات | غير مشمولة | تتطلب تصديراً مستقلاً وترحيلها إلى MySQL/TiDB خارجي |
| خدمات Manus المدمجة | تحتاج بديلاً | تشمل التخزين، OAuth، الإشعارات وخدمات الذكاء الاصطناعي |

## متطلبات الاستضافة

تحتاج الاستضافة إلى Node.js 22 أو إصدار متوافق، ومدير عمليات مثل PM2 أو آلية تشغيل دائمة، وMySQL/TiDB يمكن الوصول إليه عبر `DATABASE_URL`. كما يلزم إعداد reverse proxy أو إعدادات منصة الاستضافة لتمرير HTTPS إلى المنفذ الذي يحدده المتغير `PORT`.

| الإعداد | القيمة التشغيلية |
|---|---|
| التثبيت | `pnpm install --frozen-lockfile` |
| بناء الواجهة والخادم | `pnpm build` |
| التشغيل | `pnpm start` |
| فحص الأنواع | `pnpm check` |
| الاختبارات | `pnpm test` |

## متغيرات البيئة المطلوبة

تُنشأ هذه القيم من لوحة الاستضافة الخارجية أو من مدير أسرارها، ولا تُحفظ في ملفات الحزمة. قيمة `DATABASE_URL` هي اتصال قاعدة البيانات الخارجية، وقيمة `JWT_SECRET` مفتاح عشوائي طويل خاص ببيئة الإنتاج. أما المتغيرات المرتبطة بـManus فيجب استبدال خدماتها أو إعادة إعدادها قبل الإطلاق الخارجي.

| المتغير | الغرض |
|---|---|
| `NODE_ENV` | يضبطه إلى `production` في بيئة الإنتاج |
| `PORT` | المنفذ الذي يستمع عليه خادم Node.js |
| `DATABASE_URL` | اتصال MySQL أو TiDB الخارجي |
| `JWT_SECRET` | توقيع الجلسات بصورة آمنة |
| `VITE_APP_ID` و`OAUTH_SERVER_URL` | يتطلبان بديلاً لخدمة OAuth المدمجة |
| `BUILT_IN_FORGE_API_URL` و`BUILT_IN_FORGE_API_KEY` | يتطلبان بديلاً لخدمات التخزين والإشعارات المدمجة |

## خطوات النقل المقترحة

يُنقل المصدر أولاً إلى بيئة اختبار مستقلة، ثم تُنشأ قاعدة بيانات منفصلة ويُطبّق مخطط Drizzle بعد مراجعة الترحيلات. بعد ذلك تُضبط متغيرات البيئة الفعلية في لوحة الاستضافة وليس في ملفات المشروع، ثم يُبنى التطبيق ويشغّل على نطاق اختبار. لا يُحوّل الدومين الرئيسي إلا بعد اختبار الحجز ولوحة الإدارة وإشعارات الخطأ من الاستضافة الجديدة.

## التبعيات التي تحتاج قراراً قبل الإطلاق الخارجي

الإصدار الحالي يستخدم خدمات مستضافة ضمن المنصة لإدارة OAuth وتخزين الملفات وبعض الإشعارات والخدمات المساعدة. عند النقل إلى Hostinger أو أي استضافة خارجية، يجب استبدال هذه الخدمات أو إعادة ربطها بمزود مستقل. لا تنسخ القيم السرية الحالية ولا تُرفع إلى الاستضافة الجديدة.

> لا تكفي استضافة ملفات PHP أو الاستضافة المشتركة العادية لتشغيل هذه الحزمة. يجب أن تتيح الباقة تشغيل تطبيق Node.js بشكل مستمر واتصالاً بقاعدة بيانات خارجية أو مُدارة.
