# نشر الإصدار الموحد v11 على Hostinger

هذه الحزمة هي إصدار واحد يجمع **نظام الحجز الديناميكي** وتحسينات **SEO**. لا ترفع أي حزمة سابقة فوقها.

## الترتيب الإلزامي

| الترتيب | الإجراء | النتيجة المطلوبة |
|---|---|---|
| 1 | إنشاء نسخة احتياطية من قاعدة بيانات Hostinger عبر phpMyAdmin | إمكانية الرجوع عند الحاجة |
| 2 | استيراد `hostinger_dynamic_booking_upgrade.sql` مرة واحدة في قاعدة `u694781553_evan_booking` | إضافة علاقات الأطباء والفروع والخدمات، منع تعارض المواعيد، وسجل تدقيق الإدارة |
| 3 | رفع ملف المصدر ZIP للإصدار v11 إلى Web Apps واستبدال المصدر السابق | تطبيق واحد متسق للأصول والواجهة والخادم |
| 4 | تنفيذ البناء وتشغيل التطبيق من لوحة Web Apps | إصدار الموقع الجديد يعمل على الدومين الرسمي |
| 5 | إجراء فحوصات ما بعد النشر أدناه | تأكيد الحجز والفهرسة قبل إبلاغ محركات البحث |

> لا تُنفّذ ملف SQL مرتين. إذا ظهر تنبيه بوجود جدول أو عمود أثناء الاستيراد، توقّف وراجع حالة الترحيل بدلاً من تجاوز التنبيه عشوائياً.

## إعداد Web Apps

اترك اتصال قاعدة البيانات الحالي كما هو، وتأكد من أن المضيف في `DATABASE_URL` هو `127.0.0.1` وليس `localhost`. في إعدادات التطبيق استخدم **Node.js 22**، واجعل نقطة التشغيل `dist/index.js`.

| الإعداد | القيمة |
|---|---|
| أمر التثبيت والبناء | `npm install --omit=dev && npm run build` |
| نقطة التشغيل | `dist/index.js` |
| `NODE_ENV` | `production` |
| `CANONICAL_ORIGIN` | `https://evanclinic.sa` |
| `SITE_NAME` | `مجموعة عيادات إيفان الطبية` |

الملف `.env.production` المرفق يثبت مسار الأصول على `/assets`، لذلك لا تُزل أو تعدّل قيمته. لا تضع أي كلمة مرور أو مفتاح سري داخل ملف ZIP.

## فحوصات ما بعد النشر

بعد نجاح البناء، افتح كل مسار مباشرة في نافذة خاصة أو بعد تحديث قاسٍ للمتصفح:

| المسار | التحقق المطلوب |
|---|---|
| `https://evanclinic.sa/booking` | يبدأ اختيار الموعد بالفرع ثم نوع الرعاية والخدمة والطبيب والوقت |
| `https://evanclinic.sa/go/mahdiyah` | تظهر صفحة الإعلان بصورة الفرع وأزرار الحجز والاتجاهات بلا قوائم الموقع |
| `https://evanclinic.sa/robots.txt` | يظهر مرجع خريطة الموقع الصحيحة |
| `https://evanclinic.sa/sitemap.xml` | تظهر روابط الصفحات العامة والفروع |
| `https://evanclinic.sa/llms.txt` | يظهر وصف المجموعة والفروع والتخصصات بصيغة واضحة للمساعدات الذكية |
| `view-source:https://evanclinic.sa/` | يظهر العنوان والوصف وJSON-LD داخل HTML الخام |

يوفر هذا الإصدار محتوى قابلاً للفهرسة وبيانات منظمة وملفاً إرشادياً للمساعدات الذكية، لكنه لا يضمن ظهور الموقع في إجابة معينة من أي خدمة ذكاء اصطناعي؛ قرار الاستشهاد يبقى لتلك الخدمة ومحركات البحث.
