# نشر إصلاح الاعتمادات الأمنية — Hostinger v17

هذه الحزمة تعالج الاعتمادات التي رصدها فحص Hostinger. لا تتضمن ترقية لهيكل قاعدة البيانات، لذلك **لا تستورد أي ملف SQL** في هذه الخطوة.

## طريقة التطبيق

1. نزّل أرشيف `evan-medical-group-hostinger-v17-security-update.zip` وخذ نسخة احتياطية من ملفات التطبيق الحالي.
2. استبدل ملفات مصدر التطبيق بمحتويات الأرشيف، مع التأكد أن `package.json` و`pnpm-workspace.yaml` في جذر التطبيق.
3. من Terminal التطبيق، شغّل:

   ```bash
   rm -rf node_modules
   npm install --omit=dev
   npm run build
   ```

4. أعد تشغيل التطبيق في Hostinger، ثم افتح الصفحة الرئيسية ونموذج الحجز.
5. من صفحة **Vulnerabilities** في Hostinger اختر إعادة الفحص أو انتظر الفحص التالي. إذا بقي تنبيه، أرسل تفاصيله النصية أو لقطة كاملة.

## ما لا ينبغي فعله

لا تشغّل `npm audit fix --force` داخل Hostinger، ولا تعِد استيراد أي من ملفات SQL للحجز أو WhatsApp. لا تغيّر متغيرات WhatsApp الحالية ضمن هذا التحديث.
