# نشر إصدار Hostinger v15 — إصلاحات الفحص وتحديثات الصفحات

## ما الذي يتضمنه الإصدار؟

يجمع هذا الإصدار التحديثات التي تمت بعد v14: استبدال سلوغن افتتاحية الصفحة الرئيسية، معرض صفحة **عن المجموعة** بفرع واحد في كل شريحة مع الاحتفاظ بصورة العليا النهارية، تحسين النصوص وCTA وتعريب صفحة 404، واستبدال اختصار اسم طبيب الأسنان برمز طبي محايد في صفحة الأحمدية.

كما يصحح إصدار v15 خريطة الموقع: لا تعرض `sitemap.xml` بعد النشر مسارات الحجز أو صفحات الإعلان أو الصفحات التي تحمل `noindex`، بينما تبقى الصفحات الأساسية والفروع الدائمة قابلة للفهرسة.

## طريقة الرفع

1. افتح **Hostinger hPanel → Websites → Web Apps** للتطبيق المرتبط بـ `evanclinic.sa`.
2. ارفع ملف ZIP الخاص بـv15 بدلاً من الحزمة السابقة.
3. استخدم Node.js 22 ونقطة التشغيل `dist/index.js`.
4. نفذ أمر البناء المحدد في التطبيق: `npm install --omit=dev && npm run build`.
5. انتظر حالة بناء ناجحة، ثم افتح الصفحة الرئيسية و`/about` و`/sitemap.xml` في نافذة خاصة أو بعد تحديث قوي.

> لا يتضمن هذا الإصدار ترحيلاً جديداً لقاعدة البيانات. لا تُعد استيراد ملفات SQL السابقة ما لم يكن هناك طلب صريح لتعديل بيانات الحجز.

## فحص ما بعد النشر

تأكد من ظهور السلوغن **«رعاية متخصصة تبدأ من فهم احتياجك.»** في الصفحة الرئيسية، ومن أن معرض `/about` يعرض صورة واحدة لفرع العليا (النهارية). ثم افتح `/sitemap.xml` وتأكد من غياب مسارات `/go/` و`/booking`.
