# ملاحظات ربط نطاق Evan Clinic

تمت مراجعة لقطة إعدادات DNS في 19 أغسطس 2026.

- السجل `ALIAS` بالاسم `@` يشير إلى `cname.manus.space`، ولا ينبغي حذفه أثناء إكمال الربط.
- السجل `CNAME` بالاسم `www` ما زال يشير إلى `www.evanclinic.sa.cdn.hstgr.net`، وهو سجل الاستضافة السابقة المتعارض مع إضافة سجل `www` جديد.
- سجلات البريد `MX` و`TXT` الخاصة بالاسم `@`، وكذلك السجلات الفرعية للبريد، يجب أن تبقى دون تغيير.
- الإجراء التالي هو تعديل سجل `www` القائم بدلاً من إضافة CNAME ثانٍ بالاسم نفسه، مع مطابقة القيمة النهائية مع القيمة التي تعرضها إعدادات نطاق المشروع.
