# نشر إصدار Hostinger v13

يتضمن هذا الإصدار إصلاحاً ضرورياً للفهرسة: يمنع تقديم `index.html` الخام قبل أن يضيف الخادم العنوان والوصف و`canonical` وJSON-LD الخاصين بكل صفحة. بعد النشر، يجب أن يختفي الوسم `<!--seo-head-->` من HTML الخام للصفحة الرئيسية.

## الخطوات

1. احتفظ بنسخة احتياطية من قاعدة البيانات قبل أي استيراد.
2. إن لم يكن قد تم سابقاً، استورد `hostinger_dynamic_booking_upgrade.sql` مرة واحدة فقط.
3. إن لم يكن قد تم سابقاً، استورد `hostinger_care_departments_placeholder_seed.sql` مرة واحدة فقط لإتاحة سجلات الجلدية والليزر المؤقتة.
4. في Hostinger **Web Apps**، ارفع مصدر الإصدار v13 ZIP بدلاً من الحزمة السابقة.
5. استخدم Node.js 22، وأمر البناء `npm run build`، وملف الدخول `dist/index.js`.
6. تأكد من أن `DATABASE_URL` مضبوط على `127.0.0.1` وليس `localhost`، وأن `VITE_ASSET_BASE=/assets` موجود في `.env.production` ضمن الحزمة.
7. بعد نجاح البناء، افتح `https://evanclinic.sa/` في نافذة خاصة وتحقق من صفحة الحجز.

## تحقق SEO بعد النشر

افتح مصدر الصفحة أو نفّذ فحص HTML خام. يجب أن تجد:

- عنوان الصفحة ووصفها ووسم canonical.
- JSON-LD من النوع `MedicalClinic`.
- محتوى عربي ظاهر في عنصر `#root` قبل تشغيل JavaScript.
- ملفي `https://evanclinic.sa/robots.txt` و`https://evanclinic.sa/sitemap.xml`.

لا يمكن لأي تهيئة تقنية ضمان إدراج الموقع في جواب كل نموذج ذكاء اصطناعي، لكنها تجعل النسخة الرسمية قابلة للاكتشاف والاقتباس من الزواحف التي تسمح سياساتها بذلك.
