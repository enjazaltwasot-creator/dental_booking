# عقد مزامنة محادثات مساعد إيفان مع CRM

## الغرض والحالة الحالية

يسجل الموقع الآن كل تبادل جديد بين المراجع ومساعد إيفان داخل جلسة معرفّة برمز عشوائي خاص بالمتصفح. ينشئ النظام أو يحدّث حدثاً داخلياً من نوع `assistant_conversation` بحالة `pending`. لا ينفذ الموقع أي طلب شبكي إلى CRM أو واتساب في المرحلة الحالية.

> الغرض من هذا السجل هو تمكين فريق التشغيل من مراجعة السياق عند الحاجة، ثم تسليم آخر سياق للمحادثة إلى CRM بعد اعتماد المزود والحقول وسياسة الاحتفاظ بالبيانات.

| العنصر | القيمة الحالية |
|---|---|
| القناة | `website`، مع حجز قيمة `whatsapp` للتكامل اللاحق |
| معرّف الجلسة | `sessionKey` محلي محفوظ في المتصفح؛ لا يرتبط تلقائياً باسم أو رقم جوال |
| الرسائل المسجلة | آخر رسالة من المراجع ورد مساعد إيفان عليها |
| حالة حدث المزامنة | `pending` حتى ربط CRM المعتمد |
| الوصول في الموقع | المسؤولون المسجلون فقط عبر لوحة الإدارة |

## نموذج الحدث الداخلي

يكون `resourceReference` دائماً بالشكل `assistant:{conversationId}`. يحافظ القيد الفريد على حدث واحد لكل جلسة ويستبدل حمولته بأحدث سياق مسجل؛ لذلك يجب على موصل CRM قراءة الرسالة عند وجود الحالة `pending`، ثم تعيين الحالة إلى `sent` مع حفظ مرجع المزود.

```json
{
  "channel": "website",
  "conversationId": 42,
  "sessionKey": "website-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
  "lastMessageAt": "2026-08-16T14:00:00.000Z",
  "messages": [
    {
      "role": "user",
      "content": "أبي أحجز في العليا",
      "createdAt": "2026-08-16T14:00:00.000Z"
    },
    {
      "role": "assistant",
      "content": "هلا بك، تقدر تبدأ من صفحة الحجز.",
      "createdAt": "2026-08-16T14:00:00.000Z"
    }
  ]
}
```

## متطلبات التفعيل اللاحق

| مطلوب من CRM | سبب الحاجة |
|---|---|
| عنوان الاستقبال أو مواصفات API | تحديد طريقة تسليم حدث المحادثة |
| طريقة المصادقة | حفظ بيانات الوصول في إعدادات المشروع بدلاً من الشيفرة |
| خريطة الحقول | ربط معرف الجلسة والقناة والرسائل بالكيان الصحيح في CRM |
| سياسة الاحتفاظ والتنقيح | تحديد مدة الاحتفاظ بالمحادثات وكيفية التعامل مع المحتوى الحساس |
| قواعد إعادة المحاولة | معالجة أحداث `failed` ومنع التكرار عند نجاح الإرسال |

## حدود التشغيل

يحفظ سجل المحادثة السياق التشغيلي ولا يمثل ملفاً طبياً، ولا يجب استخدامه لتشخيص أو توثيق قرار علاجي. وتبقى مزامنة واتساب منفصلة حتى تزويد المشروع بإعدادات Meta المعتمدة وبيانات القناة.
