# دليل نشر Hostinger — الإصدار v16

يجمع هذا الإصدار بين **تأكيد استلام طلب الحجز عبر قالب WhatsApp Utility** وتحسين تحميل الصفحة الرئيسية على الجوال. لا تضم الحزمة أي رموز وصول أو كلمات مرور أو بيانات مراجعين.

## ما أضيف في الإصدار

| المجال | التنفيذ |
|---|---|
| تأكيد WhatsApp | موافقة اختيارية صريحة في نموذج الحجز، وقالب `booking_request_received` بالمتغيرين `customer_name` و`booking_reference`، وسجل إرسال يمنع التكرار. |
| الخصوصية | لا يسجل جدول الإرسال رقم الجوال أو نص الرسالة؛ يحتفظ ببصمة رقمية وحالة تقنية ومعرّف Meta فقط. |
| مرونة الحجز | فشل WhatsApp لا يمنع حفظ طلب الحجز أو ظهور رقمه المرجعي. |
| أداء الجوال | صورة WebP افتتاحية صغيرة عالية الأولوية، وتأخير فيديو الخلفية على الجوال، وفصل صفحات الموقع والمساعد الذكي عن JavaScript الأولي. |

## ترتيب النشر الصحيح

1. خذ نسخة احتياطية من قاعدة بيانات Hostinger.
2. استورد **مرة واحدة فقط** الملف `hostinger_whatsapp_booking_confirmation_upgrade.sql` من phpMyAdmin. لا تعِد استيراد ترقية الحجز الديناميكي أو ملف خدمات الجلدية والليزر السابقين.
3. فك ضغط الحزمة الجديدة وارفع محتوياتها مكان مصدر التطبيق الحالي في Hostinger.
4. تأكد من استخدام **Node.js 22** ومن أن ملف البداية هو `dist/index.js`.
5. نفّذ في الطرفية:

   ```bash
   npm install --omit=dev
   npm run build
   ```

6. أعد تشغيل التطبيق.

## إعدادات Hostinger المطلوبة

| المتغير | القيمة/الغرض |
|---|---|
| `WHATSAPP_ACCESS_TOKEN` | رمز الوصول الحالي المحفوظ في إعدادات Hostinger السرية؛ لا يوضع في أي ملف. |
| `WHATSAPP_PHONE_NUMBER_ID` | `1101566993047104` — معرّف رقم الإرسال المعتمد فقط. |
| `WHATSAPP_TEMPLATE_LANGUAGE` | `ar`، ما لم تُظهر Meta رمز لغة مختلفاً للقالب النشط. |
| `WHATSAPP_API_VERSION` | اختياري؛ الافتراضي في الكود هو `v18.0`. |

> لن يتم إرسال رسالة تلقائياً إلا للطلب الجديد الذي يحدد صاحبه مربع الموافقة. يظل الإرسال معطلاً عملياً حتى يكون رقم الأعمال مسجلاً في Cloud API؛ نتيجة اختبار Meta السابقة كانت `133010`، أي إن التسجيل مطلوب قبل إعادة الاختبار.

## التحقق بعد النشر

افتح الصفحة الرئيسية على الجوال وتأكد أن الافتتاحية تظهر مباشرة، ثم تأكد من أن الحركة تعود بعد وقت قصير. افحص `/booking` حتى خطوة البيانات: يجب أن يظهر مربع موافقة WhatsApp غير محدد مسبقاً. بعد تسجيل رقم الأعمال في Meta، يمكن إجراء اختبار واحد فقط إلى رقم مملوك أو مفوض من صاحب المشروع بعد موافقته الصريحة.

افحص كذلك `https://evanclinic.sa/sitemap.xml` بعد الرفع؛ لا ينبغي أن يحتوي روابط `/go/` أو `/booking` أو مسودات المدونة غير المفهرسة.
